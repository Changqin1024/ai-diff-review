# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] - 2026-09-20

### 新增
- 逐块接受 / 拒绝，支持按文件与全局（全部接受 / 全部拒绝）操作。接受 = 把基线推进到当前内容，拒绝 = 把磁盘回退到基线。
- 侧边栏 **待审查改动** 树视图（按 **文件夹层级嵌套**：文件夹 → 文件 → 改动块），点击改动块可跳转定位；**全局操作** 视图；整页 **设置** 视图。
- 审查面板：单文件、显示整份文件内容（可切换为仅变更片段）、实时刷新；处理完后继续显示该文件操作后的最终内容。
- 原生 diff 编辑器：悬停的块级接受 / 拒绝、标题栏文件级按钮；文件审查完毕后其 diff 标签页自动关闭。
- 工作区级设置：启动监听、监听路径（动态生效，全部失效时回退整个工作区）、排除项（加入排除会清除对应基线）。
- 缓存目录自定义（更改时**迁移**已有基线而非复制）；一键清理当前工作区缓存。
- 二进制文件追踪开关（默认关；关闭后**延迟**删除已存的二进制基线）。
- diff 显示范围：整份文件 / 仅变更片段。
- 编码识别（UTF-8、GBK/GB2312、GB18030、Big5、Shift_JIS、EUC 等），中文不再乱码，拒绝回写保持原编码；可选用 VS Code 的 `files.autoGuessEncoding`。
- 重命名 / 移动时**搬迁基线**（Git 重命名检测优先，内容哈希兜底）。
- 命令：记录基线、重置基线、打开文件、在资源管理器中显示、打开原生对比等；自定义扩展图标。
